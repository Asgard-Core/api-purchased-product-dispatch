package co.com.asgard.core.repository;

import co.com.asgard.core.model.ListaDespacho;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ListaDespachoRepository extends JpaRepository<ListaDespacho, Long> {
}